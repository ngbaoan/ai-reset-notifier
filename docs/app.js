async function L(){const d=await fetch('dashboard.json?'+Date.now()).then(r=>r.json());window.A=d.accounts;R();setInterval(R,1000);}function F(ms){let s=Math.max(0,ms/1000|0),h=(s/3600|0),m=((s%3600)/60|0),x=s%60;return[h,m,x].map(v=>String(v).padStart(2,'0')).join(':')}function R(){let n=Date.now();list.innerHTML='';A.forEach(a=>{let d=new Date(a.resetAt)-n;let st=d<=0?'<span class=dot></span>':'<span class=time>⏳'+F(d)+'</span>';list.innerHTML+=`<div class=row><div>${a.account}</div><div>${st}</div><div>${a.task}</div></div>`})}L();setInterval(L,30000);